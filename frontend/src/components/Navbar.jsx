function Navbar() {
    return (
        <nav className="nav">
            <div className="links">
                <a href="#">Link</a>
                <a href="#">Link</a>
                <a href="#">Link</a>
            </div>
        </nav>
    );
}

export default Navbar;